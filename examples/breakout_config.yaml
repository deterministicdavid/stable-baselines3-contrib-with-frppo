# config.yaml
# Configuration for PPO CarRacing training

# --- General Env ---
env_name: "ALE/Breakout-v5"
n_stack: 4

# --- Training Parameters ---
train:
  algo: "PPO"
  policy: "own" # either "own" or "default"
  n_envs: 8 # Default number of parallel environments
  n_steps: 1024 # number of steps to do in an environment
  batch_size: 1024
  total_timesteps: 100000000
  fr_tau_penalty: 0.00001
  fr_scale_by_adv: true
  ent_coef: 0.0
  decay_lr: true

# --- Logging & Saving ---
logging:
  log_dir: "./logs_breakout/"
  name_prefix: "ppo_breakout_latest"
  checkpoint_save_freq: 10000 # OverwriteCheckpointCallback save frequency

# --- Visualization ---
visualize:
  video_folder: "videos_breakout/"
  deterministic: true
